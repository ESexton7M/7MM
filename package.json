{
  "name": "asana-analytics-dashboard",
  "private": true,
  "version": "0.1.0",
  "main": "app.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "install:all": "npm install && cd analyzer && npm install",
    "build": "cd analyzer && npm run build",
    "dev": "cd analyzer && npm run start",
    "lint": "cd analyzer && npm run lint"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
