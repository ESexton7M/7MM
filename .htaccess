# .htaccess disabled for Plesk Node.js
# All routing is handled by the Node.js application (startup.js/app.js)
# This file intentionally left minimal to avoid conflicts

# Do NOT add RewriteEngine or RewriteRule directives
# They will cause redirect loops with Plesk Node.js